# requirements-dev.txt
# Dev/test dependencies for webmarket. Installed alongside runtime deps
# when a developer runs:  pip install -r requirements.txt
# Production-only deployers should install requirements-prod.txt instead.

# Test runner + plugins
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
pytest-mock==3.14.0
freezegun==1.5.1

# Async MongoDB in-memory backend for unit/integration tests
mongomock-motor==0.0.36

# Async HTTP client used by FastAPI's TestClient / httpx.AsyncClient
httpx==0.27.2
